import SwiftUI

struct StickyNoteDetail: View {
    var instance: WidgetInstance
    var context: WidgetContext

    var body: some View {
        Text("StickyNoteDetail")
            .font(.system(size: 12))
            .foregroundStyle(.secondary)
    }
}
