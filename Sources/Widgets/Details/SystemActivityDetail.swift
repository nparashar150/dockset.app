import SwiftUI

struct SystemActivityDetail: View {
    var instance: WidgetInstance
    var context: WidgetContext

    var body: some View {
        Text("SystemActivityDetail")
            .font(.system(size: 12))
            .foregroundStyle(.secondary)
    }
}
