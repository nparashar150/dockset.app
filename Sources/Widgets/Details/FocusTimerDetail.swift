import SwiftUI

struct FocusTimerDetail: View {
    var instance: WidgetInstance
    var context: WidgetContext

    var body: some View {
        Text("FocusTimerDetail")
            .font(.system(size: 12))
            .foregroundStyle(.secondary)
    }
}
